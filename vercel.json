{
  "version": 2,
  "builds": [
    {
      "src": "api/server.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/server.py"
    }
  ],
  "installCommand": "apt-get update && apt-get install -y chromium chromium-driver"
}
